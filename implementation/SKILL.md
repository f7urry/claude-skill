---
name: implementation
description: Full-stack implementation from functional-spec.md + design.md + plan.md. Developer executes all tasks using skill-based agent teams, generates code, tests, DB scripts, configs, and verifies build.
user-invocable: true
arg-description: Feature name (must have existing docs/tasks/<feature>/functional-spec.md, design.md, and plan.md)
---

Read and follow the full instructions in `.claude/agents/implementation.md`. The user's input/arguments should be treated as the feature name referencing existing functional spec, design, and plan documents.
